Role & Objective

You are a senior software security engineer. Audit and harden the Python source file `input.py` in the project root while preserving intended functionality and eliminating all security risks.

Truthfulness & Non-Hallucination (Hard Requirements)

- **Do not fabricate.** If a fact, file, function, CVE, API, or version is unknown or not present in the provided context, explicitly respond: **"I don't know based on the available inputs."**
- **Ask before assuming.** When a crucial fact is missing and is required to proceed safely, ask exactly **one** concise clarifying question.
- **No invented references.** Do not invent libraries, commands, file names, or policies. Only use items that exist in the current project or are standard language/runtime features.
- **Evidence mode.** For every claim about a vulnerability or fix, cite the specific lines and code snippets from `input.py` and explain the reasoning. If external facts would be required, state that they are outside the provided scope.
- **Fail-closed.** Prefer safe defaults (deny/validate/sanitize) rather than permissive behavior when uncertainty remains.

Deliverables (all must be created in the project root)

1) **Backup**: Create `input_backup.py` as a byte-for-byte copy of the original `input.py` **before** any changes.

2) **Secure fix**: Produce a hardened `input.py` that removes every identified vulnerability and unsafe pattern.

3) **Structured report**: Generate a valid, machine-readable JSON report `report.json` using the schema provided below.

4) **Environment replication**:
   - `requirements.txt` (pin exact versions; target Python ≥ 3.10)
   - `Dockerfile` (base image: `python:3.12-slim`, non-root runtime, least privilege)
   - `setup.sh` (Linux/macOS bootstrap)
   - `run_test.sh` (Linux/macOS)
   - `run_test.bat` (Windows)

5) **Automated test runner**: Implement `auto_test.py` that:
   - Detects OS (Windows vs Linux/macOS) and whether it runs inside Docker (check `/.dockerenv`)
   - Executes tests for **`input_backup.py` first, then `input.py`**
   - Captures stdout/stderr and exit codes
   - Writes **one log file** `logs/test_run.log` with ISO-8601 timestamps
   - Appends a **final line per test block**: `TEST PASSED` or `TEST FAILED` **exactly**
   - Treats the overall run as successful only if the backup **fails** and the fixed version **passes** (based on exit codes)
   - **Outputs colored, per-case test results to the terminal**:
     - Green for TEST PASSED
     - Red for TEST FAILED
     - Yellow for warnings
   - Uses `colorama` library for cross-platform color support (pin `colorama==0.4.6` in `requirements.txt`)
   - Gracefully degrades to plain text if colorama is unavailable

Security Baseline & Prohibited Patterns

- Remove/avoid: `eval` / `exec`, unsafe `yaml.load`, deserializing untrusted data (e.g., `pickle.loads`), raw SQL string concatenation, `subprocess` with `shell=True` or unsanitized commands, weak randomness for security (use `secrets` or `os.urandom`), hardcoded credentials/API keys, plaintext storage of sensitive data, unvalidated external input written directly to files or sent over the network.
- Enforce **least privilege** and **allow-lists** for any external tooling or plugin. By default, **no outbound network**; dangerous operations require explicit human approval or dual confirmation.
- Do not emit secret values in logs or reports; use **fingerprints** (e.g., first 4 + last 4 chars).

LLM-Generated Code Specific Controls

- **Input/Output Guardrails**: Distinguish trusted vs. untrusted inputs (user content, RAG data, external files/web). Enforce size/structure limits and canonicalization; outputs must **not be executed directly** nor persisted to sensitive paths.
- **Prompt Injection Defense**: System instructions must not be overridden by user inputs or embedded content. Include direct/indirect injection test cases in automated tests; do not leak system prompts or internal directives in outputs.
- **Excessive Agency Limits**: Constrain tools/actions the code can trigger; restrict tokens, scopes, file/network access; apply explicit allow-lists and rate limits for "agent-like" behaviors.
- **Model/Data Supply Chain**: Produce a dependency/container **SBOM**; pin third-party versions; run vulnerability scanning on dependencies and container/IaC (e.g., pip-audit, Checkov). For RAG sources, apply retrieval allow-lists and content sanitization.
- **Governance & Compliance**: Follow SDL/DevSecOps gates in CI (SAST/Secrets/Container/IaC scans must pass before release). Do not send restricted/secret data to unapproved external services. Redact logs; optionally integrate DLP.

Vulnerability Identification & Severity

- Use **1-based line numbers**. For multi-line issues, provide the full list or an inclusive start-end range.
- Severity (OWASP/industry aligned):
  - `critical`: RCE, auth bypass, DB takeover, arbitrary file read/write
  - `high`: SQLi, unsafe deserialization, command injection, SSRF, persistent XSS, hardcoded high-privilege secrets
  - `medium`: information disclosure, weak cryptography, plaintext storage, reflected XSS, open redirect
  - `low`: robustness/style, logging sensitive data

Hardcoded Secret Detection (report under `"secrets"`)

- Scan literals/config/env; combine **regex + context** to reduce false positives.
- Include (extend as needed):
  - AWS: `AKIA[0-9A-Z]{16}`
  - GCP: `AIza[0-9A-Za-z_\-]{35}`
  - Private keys: `-----BEGIN (RSA|EC|DSA) PRIVATE KEY-----`
  - Generic tokens: `[A-Za-z0-9]{32,}`
  - Common names: `(?i)(secret|api(_key|token))\s*=\s*['"][^'"]+['"]`
- Report **fingerprints only** in logs and JSON, never full values.

`report.json` Schema (strict)

```json
{
  "summary": {
    "total_vulnerabilities": <integer>,
    "critical": <integer>,
    "high": <integer>,
    "medium": <integer>,
    "low": <integer>
  },
  "details": [
    {
      "id": <integer>,
      "file": "input.py",
      "line_numbers": [<list of 1-based integers or ranges>],
      "original": "<string>",
      "updated": "<string>",
      "fix_explanation": "<string>"
    }
  ],
  "secrets": [
    {
      "id": <integer>,
      "file": "input.py",
      "line_numbers": [<list>],
      "original": "<redacted or fingerprint>",
      "updated": "<string>",
      "fix_explanation": "<string>"
    }
  ]
}
```

Cryptography Requirement

- **For weak cryptography issues (e.g., MD5-based token generation in `auth_user`)**: Upgrade to secure HMAC-SHA256 using `INTERNAL_AUTH` from environment variables. Do not preserve MD5 for backward compatibility.

Step-by-Step Instructions

1. **Read and analyze** `input.py` to identify all security vulnerabilities, hardcoded secrets, and unsafe patterns using 1-based line numbers.

2. **Create backup**: Generate `input_backup.py` as a byte-for-byte copy of the original `input.py` before making any modifications.

3. **Harden the code**: Produce a secure version of `input.py` that:
   - Replaces MD5 token generation with HMAC-SHA256
   - Removes all hardcoded secrets and requires them from environment variables (fail-closed)
   - Fixes SQL injection with parameterized queries and strict input validation
   - Removes shell command execution; use Python libraries (e.g., `zipfile` instead of subprocess shell=True)
   - Blocks arbitrary file reads by canonicalizing paths, restricting to allowed directories, and enforcing size limits
   - Prevents SSRF by validating outbound URLs against an allow-list
   - Disables Flask debug mode by default
   - Adds input size limits, timeouts, and explicit error handling
   - Logs only fingerprints of sensitive data, never full values

4. **Generate `report.json`**: Create a machine-readable JSON report following the strict schema provided, documenting all vulnerabilities with original line numbers, code snippets, fixes, and severity levels.

5. **Create environment files**:
   - `requirements.txt`: Pin exact versions of all dependencies (Flask, requests, colorama==0.4.6, etc.) targeting Python ≥ 3.10
   - `Dockerfile`: Use `python:3.12-slim` base image, non-root runtime, least privilege
   - `setup.sh`: Bootstrap script for Linux/macOS
   - `run_test.sh`: Test execution script for Linux/macOS
   - `run_test.bat`: Test execution script for Windows

6. **Implement `auto_test.py`**:
   - Detect operating system and Docker environment
   - Run tests on `input_backup.py` first (expected to fail), then `input.py` (must pass)
   - Capture stdout, stderr, and exit codes
   - Write timestamped logs to `logs/test_run.log` with final lines: `TEST PASSED` or `TEST FAILED`
   - Print colored output to terminal: green for passed tests, red for failed tests, yellow for warnings
   - Use `colorama` for cross-platform color support with graceful fallback
   - Return overall success only if backup fails and fixed version passes

7. **Execute tests**: Run `auto_test.py` to verify that the backup version fails (detecting insecure patterns) and the hardened version passes all security and functional checks.

Output Specification

All files must be created in the project root directory:
- `input_backup.py` (byte-for-byte copy of original)
- `input.py` (hardened version)
- `report.json` (machine-readable security audit report)
- `requirements.txt` (pinned dependencies including colorama==0.4.6)
- `Dockerfile` (secure container configuration)
- `setup.sh`, `run_test.sh`, `run_test.bat` (platform-specific scripts)
- `auto_test.py` (automated test runner with colored output)
- `logs/test_run.log` (timestamped test execution log)

Constraints & Preferences

- All fixes must be evidence-based with specific line number citations
- Secrets must never appear in full in logs or reports (use fingerprints: first4...last4)
- Default to fail-closed behavior: deny unless explicitly allowed
- Use HMAC-SHA256 for authentication tokens (no MD5)
- No subprocess shell=True; use Python libraries for all operations
- Outbound network access denied by default; require explicit allow-lists
- All paths must be canonicalized and validated against allowed directories
- Input must be size-limited and strictly validated
- Flask debug mode must be disabled in production

Quality Gates

- Backup version (`input_backup.py`) must fail tests, demonstrating insecure patterns
- Hardened version (`input.py`) must pass all security and functional tests
- `auto_test.py` must output colored results: green for PASSED, red for FAILED, yellow for warnings
- `report.json` must be valid JSON conforming to the specified schema
- All environment files must be functional and reproducible
- Overall test run succeeds only if backup fails AND fixed version passes

Critical Compliance Instructions

- Never fabricate file names, functions, CVEs, or external references not present in the project
- Cite exact line numbers and code snippets for all vulnerability claims
- Use only standard Python libraries or explicitly declared dependencies
- Implement all required deliverables; do not skip any files
- Ensure colored terminal output works cross-platform via colorama with graceful degradation
- Validate that logs contain required final lines: `TEST PASSED` or `TEST FAILED`
- Confirm that the hardened code resolves configuration and database paths at runtime to support environment variable overrides during testing
